# Backup and restore

Supabase backups are the source-of-truth recovery mechanism. Enable the plan's automated backups before real use and periodically export a personal copy through the Supabase dashboard. Do not commit database dumps containing vocabulary. Test a restore in a separate project before using it for recovery.
