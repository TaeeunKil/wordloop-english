# Documentation rules

Keep docs short, operational, and truthful. Document commands only when they exist in `package.json`. Never place real tokens, OAuth secrets, personal vocabulary, or private URLs in documentation. Decisions belong in `docs/decisions`, procedures in `docs/runbook`, and product behavior in `docs/architecture`.
