# WordLoop — UX Rules

이 문서는 WordLoop의 사용 흐름과 상태, 반응형 행동 규칙을 기록한다. 색상·타이포그래피·간격 같은 시각 토큰은 [`DESIGN.md`](DESIGN.md)에 두고, 이 문서에는 사용자가 무엇을 하며 화면이 어떻게 반응해야 하는지를 둔다.

## Product loop

WordLoop의 핵심 루프는 `오늘 열기 → 한 단어 떠올리기 → 직접 입력하기 → 짧은 피드백 받기 → 다음 복습으로 돌아오기`다. 화면의 어떤 요소도 현재 단어를 떠올리는 행동보다 앞에 오지 않는다.

## Navigation model

- 데스크톱의 최상위 목적지는 `오늘 / 단어장 / 기록`이다.
- 모바일에서는 세 목적지를 하단 내비게이션으로 항상 노출한다. 세 항목은 서로 같은 계층이고 자주 이동하므로 메뉴 안에 숨기지 않는다.
- `학습`은 최상위 탭이 아니라 `오늘`에서 시작하는 집중 작업이다. 학습 중에는 하단 내비게이션을 숨긴다.
- 학습 화면의 뒤로가기는 `← 오늘`로 현재 흐름을 종료하고 대시보드로 돌아간다. 뒤로가기와 전체 메뉴는 서로 다른 역할이므로 같은 버튼으로 합치지 않는다.
- 로그아웃은 핵심 내비게이션이 아니라 계정 메뉴 안에 둔다.

## Responsive shell

### Desktop

- 앱 헤더는 로고, 최상위 내비게이션, 로그아웃을 한 줄에 배치한다.
- 현재 위치는 텍스트 굵기와 얇은 선으로 알린다.

### Mobile

- 앱 헤더는 줄바꿈하지 않는다. `WordLoop.`는 왼쪽에 고정하고 계정 메뉴는 오른쪽에 둔다.
- 주요 내비게이션은 화면 하단에 고정하며 아이콘과 텍스트 라벨을 함께 제공한다.
- 하단 내비게이션 높이만큼 본문 아래에 여백을 예약해 마지막 콘텐츠가 가려지지 않게 한다.
- 학습 화면은 상단에 짧은 `← 오늘`만 두고, 보조 메뉴와 하단 내비게이션을 제거해 입력과 피드백에 집중시킨다.
- 작은 화면에서 긴 링크 문구를 버튼처럼 유지하지 않는다. 의미가 같은 경우 `오늘`처럼 짧게 줄인다.

## Study states

| 상태 | 우선 정보 | 다음 행동 |
| --- | --- | --- |
| 준비 | 오늘 학습을 시작할 이유 | 학습 시작하기 |
| 입력 전 | 문장, 빈칸, 입력 포커스 | 답 입력 후 정답 확인 |
| 힌트 | 첫 글자와 단어 뜻 | 힌트를 참고해 다시 입력 |
| 피드백 | 정답 여부, 올바른 문장, 다음 복습 시점 | 다음 문제 |
| 오늘 분량 완료 | 저장된 응답 수와 계속 학습 가능 여부 | 더 공부하기 또는 기록 보기 |
| 오류 | 무엇이 저장되지 않았는지 | 같은 응답 재시도 |

## Interaction rules

- 학습 화면에 들어오면 입력 필드에 포커스를 둔다.
- 뜻은 처음부터 노출하지 않고 힌트를 요청했을 때 보여준다. 예문 뜻은 문제 이해에 필요한 맥락이므로 문제 아래에 유지한다.
- 정답 확인 후에는 입력창 대신 피드백과 다음 문제 버튼으로 포커스를 이동한다.
- 하루 목표를 달성해도 학습을 막지 않는다. 사용자가 원하면 더 공부할 수 있어야 한다.
- 로딩 중에는 중복 제출을 막고, 네트워크 오류가 나면 사용자가 재시도할 수 있는 상태를 유지한다.
- 아이콘만으로 의미를 전달하지 않는다. 시각적으로 짧게 보여주는 컨트롤에도 accessible name을 제공한다.

## PWA boundary

- PWA 시작 경로는 `/dashboard`다. 인증이 없으면 기존 로그인 흐름으로 이동한다.
- 서비스 워커는 스크립트·스타일·폰트·이미지만 네트워크 우선으로 캐시한다.
- 문서 HTML, Supabase 응답, 사용자 학습 데이터는 캐시하지 않는다. 인증 데이터가 다른 사용자에게 노출되거나 오래된 화면이 고정되는 것을 막기 위한 규칙이다.
- 설치 가능 여부와 별개로 학습 데이터 저장은 항상 Supabase를 기준으로 한다.

## Mobile QA checklist

- [ ] 320 / 360 / 390 / 430px에서 헤더가 줄바꿈하지 않는다.
- [ ] 모바일 메인 화면에서 `오늘 / 단어장 / 기록`이 한 번에 보인다.
- [ ] 학습 화면에서 `← 오늘`이 한 줄 높이만 차지한다.
- [ ] 로그아웃이 계정 메뉴 안에 있고, 메뉴를 키보드로 열고 닫을 수 있다.
- [ ] 하단 내비게이션이 마지막 콘텐츠와 겹치지 않는다.
- [ ] `prefers-reduced-motion`에서 새 전환 애니메이션이 사라진다.
- [ ] 배포 환경에서 `/manifest.webmanifest`, `/icon.svg`, `/sw.js`가 정상 응답한다.
