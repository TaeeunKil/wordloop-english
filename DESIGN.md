# WordLoop — Design System

이 문서는 WordLoop의 시각적·상호작용 기준이다. WordLoop는 단어를 한 번 저장하고 끝내는 단어장이 아니라, 매일 다시 떠올리고 틀린 기록까지 쌓아가는 개인 학습 작업공간이다.

## 1. Design Direction

### Visual thesis

따뜻한 종이 위에 검은 잉크와 한 번의 테라코타 표시만 남긴, 조용하지만 계속 손이 가는 개인 영어 학습 기록실.

### Reference composition

- **Notion**: 개인 지식 저장소처럼 단어를 모으고 검색하고 편집하는 콘텐츠 구조를 참고한다.
- **Linear**: 앱 셸, 작업공간의 밀도, 상태 위계, 키보드 친화적인 상호작용을 참고한다.
- **Claude**: 따뜻한 표면, 여유 있는 읽기 리듬, 부담 없는 학습 분위기를 참고한다.
- 블로그의 `DESIGN.md`는 WordLoop와 공유하는 브랜드 기준으로 취급한다. WordLoop는 그 기준을 학습 제품에 맞게 확장한다.

### What we keep out

- Linear의 다크 테마와 보라색 브랜드 컬러
- Notion의 파란색 CTA와 무분별한 카드 그리드
- Claude의 세리프 폰트와 장식용 일러스트
- 그라데이션, 유리 효과, 그림자 중심의 깊이 표현
- 마케팅 문구가 많은 대시보드와 숫자만 나열한 통계 화면

### Content plan

각 화면은 하나의 학습 행동에 집중한다.

1. **Landing** — WordLoop가 무엇인지 설명하고 바로 시작하게 한다.
2. **Dashboard** — 오늘 복습할 양과 다음 행동을 안내한다.
3. **Study** — 현재 단어 하나를 떠올리고 응답하게 한다.
4. **Words** — 단어를 찾고 추가하고 수정한다.
5. **Stats** — 학습 기록과 잔디를 돌아본다.

### Interaction thesis

- 첫 진입에서는 브랜드와 오늘의 학습 행동이 순서대로 나타난다.
- 학습 응답 후에는 정답 여부, 정답 문장, 다음 복습 시점을 짧은 피드백으로 보여준다.
- 잔디 셀과 단어 행은 hover보다 focus와 선택 상태가 먼저 느껴지도록 만든다.

## 2. Design Principles

- 한 화면에는 하나의 주 행동만 둔다.
- 카드보다 여백, 정렬, 얇은 선, 표면 대비로 구조를 만든다.
- 단어와 문장처럼 사용자가 읽고 기억해야 하는 콘텐츠가 UI보다 앞에 온다.
- 색상보다 크기·굵기·간격으로 계층을 만든다.
- 브랜드 포인트는 행동이나 학습 상태가 있는 곳에만 사용한다.
- 대시보드는 광고 문구가 아니라 현재 상태와 다음 행동을 말해야 한다.
- 모든 화면은 모바일에서 먼저 이해되고 데스크톱에서 넓어진다.

## 3. Color Tokens

WordLoop는 블로그의 종이색과 검정 계열을 유지하되, 앱에서는 조금 더 차분하고 읽기 쉬운 색으로 조정한다.

```css
:root {
  --background: #f4f1ea;
  --surface: #fbfaf6;
  --surface-subtle: #ebe7df;
  --surface-strong: #e1dbd0;

  --text: #211f1b;
  --text-secondary: #625e57;
  --text-muted: #89837a;
  --text-on-accent: #fffaf2;

  --line: #d9d3c8;
  --line-strong: #bdb5a9;

  --accent: #d45b3d;
  --accent-hover: #b9472f;
  --accent-soft: #f2ddd4;

  --success: #2f8066;
  --success-soft: #dcebe3;
  --danger: #b64b42;
  --danger-soft: #f2dfda;
  --warning: #a87532;
  --focus: #2879b9;

  --font: "Pretendard", system-ui, -apple-system, "Segoe UI", sans-serif;
}
```

### Color roles

- `--background`: 전체 캔버스와 빈 공간. 순백색을 사용하지 않는다.
- `--surface`: 입력·학습 응답처럼 사용자의 행동을 받는 표면. 필요한 곳에만 사용한다.
- `--surface-subtle`: 선택된 행, 보조 영역, 비활성 버튼의 미세한 배경.
- `--text`: 제목과 핵심 단어. 순수한 `#000`은 사용하지 않는다.
- `--text-secondary` / `--text-muted`: 설명, 날짜, 복습 예정 시점 등 보조 정보.
- `--accent`: 시작, 저장, 현재 선택, 핵심 링크에 사용한다. 장식용으로 반복하지 않는다.
- `--success` / `--danger`: 정답·오답과 시스템 상태에만 사용한다. 색상만으로 의미를 전달하지 않는다.
- `--focus`: 키보드 포커스 전용 색상. 최소 `2px` outline을 유지한다.

### Contribution heatmap

잔디는 같은 포인트 컬러의 명도 단계로 표현한다. 각 셀에는 접근 가능한 `aria-label`과 학습 횟수 텍스트를 제공한다.

```css
--heat-0: var(--surface-subtle);
--heat-1: #f0cfc4;
--heat-2: #e6a895;
--heat-3: #da7e68;
--heat-4: var(--accent);
```

정답 여부를 잔디 색으로 표현하지 않는다. 잔디는 학습 활동량만 나타내고, 정답·오답은 별도의 의미 있는 라벨과 상태 색으로 표시한다.

## 4. Typography

제목과 본문은 저장소에 포함된 Pretendard Variable을 우선 사용한다. 외부 CDN 폰트는 호출하지 않는다.

```css
@font-face {
  font-family: "Pretendard";
  src: url("/fonts/PretendardVariable.woff2") format("woff2");
  font-weight: 100 900;
  font-display: swap;
}
```

| Role | Size | Weight | Line height | Use |
| --- | ---: | ---: | ---: | --- |
| Display | `clamp(4rem, 14vw, 10rem)` | 700 | `.82` | 랜딩의 WordLoop |
| Page title | `clamp(2.6rem, 7vw, 5.5rem)` | 700 | `.94` | 화면 제목 |
| Section heading | `1.5rem–2.25rem` | 700 | `1.15` | 화면 안의 주요 영역 |
| Word | `clamp(2.4rem, 8vw, 5rem)` | 600 | `1.0` | 학습 중인 단어 |
| Body | `1rem–1.125rem` | 400 | `1.65` | 설명과 문장 |
| UI | `.875rem–1rem` | 600 | `1.4` | 버튼, 내비게이션, 입력 레이블 |
| Meta | `.72rem–.8rem` | 600 | `1.4` | 날짜, 상태, overline |

- 제목은 큰 크기와 좁은 줄 간격으로 힘을 준다.
- 본문은 읽기와 기억을 위해 넉넉한 줄 간격을 유지한다.
- 굵기는 기본적으로 400, 600, 700 세 단계만 사용한다.
- 영단어 자체는 문장보다 크게 보여주되, 화면 전체를 압도하지 않게 한다.
- 작은 영문 overline은 대문자와 넓은 자간을 사용하되, 한국어 핵심 문구를 가리지 않는다.

## 5. App Shell

앱 내부는 하나의 작업공간으로 느껴져야 한다.

- 데스크톱 최대 너비: `1200px`
- 기본 좌우 여백: `24px–32px`, 모바일 `16px`
- 상단 바: WordLoop 로고, `오늘 / 단어장 / 기록`, 로그아웃
- 현재 메뉴는 굵기와 얇은 선을 함께 사용한다. 색상만으로 표시하지 않는다.
- 콘텐츠는 별도의 큰 카드로 감싸지 않고, 필요할 때만 `--surface`를 상호작용 영역에 사용한다.
- 데스크톱에서는 주 작업 영역과 보조 맥락을 나란히 둘 수 있다.
- 모바일에서는 보조 맥락을 주 작업 아래로 이동한다.

## 6. Screen Rules

### Landing

- 첫 화면은 WordLoop 이름, 한 문장 약속, 로그인 CTA만으로 이해되어야 한다.
- Google·GitHub 로그인은 같은 우선순위의 두 버튼으로 제공한다.
- 학습 흐름은 세 개 이하의 짧은 단계로 설명한다.
- 장식용 이미지나 가짜 통계는 사용하지 않는다.

### Dashboard

- 화면 제목은 `오늘의 학습`처럼 작업을 직접 말한다.
- 가장 큰 영역은 오늘 복습할 단어와 `학습 시작` 행동이다.
- 연속 학습일, 전체 단어 수, 오늘 완료 수는 작은 지표 행으로 둔다.
- 최근 단어는 행 목록으로 보여주며, 카드 모자이크를 만들지 않는다.
- 데이터가 없을 때는 첫 단어를 추가하는 다음 행동을 바로 제공한다.

### Study

- 한 번에 하나의 단어만 주목시킨다.
- 뜻, 예문, 입력 필드, 제출 행동의 순서를 고정한다.
- 입력 전에는 힌트를 숨기고, 사용자가 요청했을 때만 보여준다.
- 정답 상태는 성공 메시지와 다음 복습 시점을 함께 표시한다.
- 오답 상태는 틀렸다는 사실보다 올바른 답과 다시 만날 시간을 먼저 보여준다.
- 키보드 `Enter` 제출과 명확한 포커스 이동을 지원한다.

### Words

- 기본 표현은 검색 가능한 행 목록이다.
- 행에는 단어, 뜻, 마지막 복습일, 다음 복습일, 편집 행동을 둔다.
- 추가·편집 폼은 한 화면에 하나씩 열고, 저장 후 목록으로 자연스럽게 돌아온다.
- 태그는 꼭 필요한 경우에만 사용하며, 장식용 pill을 늘리지 않는다.

### Stats

- GitHub 잔디와 같은 날짜별 활동 그리드를 주 시각 요소로 사용한다.
- 총 학습량과 정답률은 잔디 아래의 짧은 지표 행으로 보여준다.
- 잔디 셀 hover만 의존하지 않고 클릭·키보드로 날짜 상세를 확인할 수 있어야 한다.
- 성취를 과장하는 배너보다 실제 기록과 변화량을 우선한다.

## 7. Components

### Buttons

- Primary: `--accent` 배경, `--text-on-accent` 텍스트, `8px` radius
- Secondary: 투명 또는 `--surface-subtle` 배경, `1px solid var(--line)`
- Tertiary: 배경과 테두리 없이 텍스트 링크로 표현
- 최소 터치 영역은 `44px` 이상이다.
- hover는 배경을 조금 어둡게 하고 `translateY(-1px)` 이하의 작은 변화만 허용한다.
- 버튼 안에 불필요한 아이콘을 반복하지 않는다.

### Inputs

- 배경은 `--surface` 또는 `transparent`, 테두리는 `--line`을 사용한다.
- radius는 `8px` 이하로 유지한다.
- label은 항상 visible하게 두며 placeholder를 label로 대체하지 않는다.
- focus는 `--focus` outline으로 명확하게 표시한다.
- 오류는 입력 아래의 짧은 설명과 `--danger` 상태로 전달한다.

### Surfaces and cards

- 기본 레이아웃은 cardless다.
- 학습 응답, 편집 폼, 확인이 필요한 작업만 표면 컨테이너로 묶는다.
- 일반 radius는 `8px`, 강조 영역은 최대 `12px`다.
- 그림자는 기본적으로 사용하지 않는다. 떠 있는 메뉴처럼 실제로 떠 있어야 하는 요소에만 아주 약한 shadow를 사용한다.
- 화면 전체를 둥근 카드 안에 넣지 않는다.

### Lists and dividers

- 목록은 얇은 `--line`으로 행을 구분한다.
- hover 시 배경을 살짝 바꾸고, focus 시 outline을 추가한다.
- 행의 정보 순서는 제목 → 의미 → 날짜/상태 → 행동으로 고정한다.

## 8. States

- **Loading**: 짧은 텍스트와 선형 표시를 사용한다. 화면을 깜빡이게 하는 큰 skeleton은 사용하지 않는다.
- **Empty**: 현재 비어 있는 이유와 다음 행동을 한 문장씩 보여준다.
- **Success**: 정답·저장 완료·학습 기록 반영을 `--success`와 텍스트로 알린다.
- **Error**: 사용자가 다시 시도할 방법을 함께 제공한다. 원인 없는 일반 오류만 보여주지 않는다.
- **Disabled**: opacity만 낮추지 말고 왜 사용할 수 없는지 설명한다.
- **Unauthenticated**: 로그인 이유와 Google·GitHub 선택지를 한 화면에 둔다.
- **Offline or stale**: 데이터가 최신인지 확신할 수 없으면 마지막 동기화 상태를 표시한다.

## 9. Motion

- 랜딩 진입: WordLoop 이름이 `350ms` 동안 짧게 올라온다.
- 화면 전환: 콘텐츠가 `200–300ms` 안에 부드럽게 나타난다.
- 정답 피드백: 상태 영역만 `150–220ms` 동안 강조한다.
- 버튼·행 hover는 `150ms` 이내에 끝낸다.
- 자동 재생, 반복 애니메이션, 패럴랙스는 사용하지 않는다.
- `prefers-reduced-motion: reduce`에서는 이동과 transition을 제거한다.

## 10. Responsive and Accessibility

- 기준 breakpoint는 `700px` 전후로 단순하게 유지한다.
- 모바일에서는 상단 메뉴가 줄바꿈되거나 compact navigation으로 바뀐다.
- 학습 단어와 입력 필드는 모바일 첫 화면에서 바로 보여야 한다.
- 모든 버튼·링크·입력은 키보드로 접근할 수 있어야 한다.
- 포커스 표시를 제거하지 않는다.
- 색상 대비는 WCAG AA를 목표로 한다.
- 잔디·아이콘·상태 점에는 텍스트 또는 `aria-label`을 함께 제공한다.
- 서버에서 오는 단어·문장·사용자 입력은 텍스트로 안전하게 렌더링한다.

## 11. Do / Do Not

### Do

- 종이색 캔버스와 검은 활자로 긴 학습 시간을 편하게 만든다.
- 오늘 해야 할 행동을 첫 화면에서 하나만 강조한다.
- 단어와 기록을 리스트·행·구분선으로 정돈한다.
- 포인트 컬러가 사용된 이유를 설명할 수 있게 한다.
- 정답·오답·복습 예정 시점을 행동 가능한 정보로 보여준다.

### Do not

- 대시보드를 여러 개의 장식 카드로 채우지 않는다.
- 색상과 pill을 태그처럼 남발하지 않는다.
- 랜딩의 큰 제목을 앱 내부 화면에도 반복하지 않는다.
- 다크 테마, 그라데이션, 유리 효과를 기본값으로 추가하지 않는다.
- 통계 숫자만 크게 보여주고 사용자가 다음에 무엇을 해야 하는지 숨기지 않는다.

## 12. Implementation Checklist

- [ ] `globals.css`의 기존 색상·radius·button 토큰을 이 문서의 토큰으로 교체한다.
- [ ] 앱 셸 내비게이션을 `오늘 / 단어장 / 기록`으로 정리한다.
- [ ] 대시보드를 오늘의 학습 중심으로 재구성한다.
- [ ] 학습 화면에 정답·오답·복습 예정 상태를 명확히 추가한다.
- [ ] 단어장과 통계를 카드 모음이 아닌 목록·그리드 중심으로 정리한다.
- [ ] Google·GitHub 로그인 상태와 오류 메시지를 같은 시각 체계로 맞춘다.
- [ ] 모바일·키보드·reduced-motion을 검증한다.
